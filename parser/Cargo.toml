[package]
name = "melbi-parser"
edition = "2024"
version.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]

# `bumpalo` is only used by the arena builder in `tests/` for now. It moves back
# to `[dependencies]` as soon as the crate ships its own `ArenaTreeBuilder`.
[dev-dependencies]
bumpalo.workspace = true
